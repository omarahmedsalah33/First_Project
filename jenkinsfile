pipeline {
    agent any

    stages {

        stage('Build App') {
            steps {
                script {
                    // Load your external Groovy script inside script block
                    def gv = load('script.groovy')
                    gv.buildingapp()
                }
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    def gv = load('script.groovy')
                    gv.testingapp()
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    def gv = load('script.groovy')
                    gv.buildingimage()
                }
            }
        }

        stage('Deploy App') {
            steps {
                script {
                    def gv = load('script.groovy')
                    gv.deployingapp()
                }
            }
        }
    }
}
